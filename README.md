# Book Chat RAG: The Heart of a Woman

โครงการนี้คือแอปพลิเคชัน Chatbot บน Streamlit ที่สร้างขึ้นเพื่อใช้เทคนิค **Retrieval-Augmented Generation (RAG)** ในการตอบคำถามเกี่ยวกับเนื้อหาในหนังสือ "The Heart of a Woman" (โดย Baroness Orczy) โดยอิงข้อมูลจากฐานข้อมูลเวกเตอร์ **ChromaDB**

---

## 🚀 ฟีเจอร์หลัก (Features)

*   **Interactive Chat Interface:** หน้าจอแชทที่ใช้งานง่าย พัฒนาด้วย Streamlit
*   **Retrieval-Augmented Generation (RAG):** ค้นหาข้อมูลที่เกี่ยวข้องจากหนังสือเพื่อนำมาประกอบคำตอบของ AI ทำให้คำตอบถูกต้องและอ้างอิงเนื้อหาจริง
*   **Vector Database:** ใช้ **ChromaDB** ในการจัดการและค้นหาข้อมูลเวกเตอร์ (Embeddings) อย่างมีประสิทธิภาพ
*   **Models:**
    *   **Retriever:** `SentenceTransformer (all-mpnet-base-v2)` สำหรับแปลงข้อความเป็นเวกเตอร์
    *   **Generator:** `Mistral-7B-Instruct-v0.2` (ผ่าน Hugging Face Transformers) สำหรับการสร้างคำตอบที่ลื่นไหลเป็นธรรมชาติ

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
RAG-BookChat/
├── app.py                  # ไฟล์หลักสำหรับรันแอปพลิเคชัน Streamlit
├── chroma_db_optimized/    # ฐานข้อมูล Vector Database (ChromaDB)
├── notebook/               # สมุดบันทึก Jupyter สำหรับการเตรียมข้อมูล
│   ├── chuckstovec.ipynb   # โค้ดสำหรับสร้าง embeddings และบันทึกลง ChromaDB
│   └── datasets/           # ไฟล์เนื้อหาหนังสือต้นฉบับ (.txt)
└── README.md               # เอกสารประกอบโครงการนี้
```

---

## 🛠️ การเริ่มต้นใช้งาน (Getting Started)

### 1. ข้อกำหนดเบื้องต้น (Prerequisites)

*   **Python:** รุ่น 3.8 หรือสูงกว่า
*   **GPU:** แนะนำให้ใช้การ์ดจอ NVIDIA ที่รองรับ CUDA เพื่อประสิทธิภาพในการรันโมเดลภาษา (LLM)
*   **RAM:** ควรมี RAM อย่างน้อย 16GB หากรันโมเดลขนาด 7B

### 2. การติดตั้ง (Installation)

ติดตั้งไลบรารีที่จำเป็นทั้งหมดด้วยคำสั่ง:

```bash
pip install streamlit chromadb sentence-transformers transformers torch accelerator
```

> [!NOTE]
> หากคุณใช้ Windows และต้องการใช้ GPU อย่าลืมติดตั้ง [PyTorch เวอร์ชันที่รองรับ CUDA](https://pytorch.org/get-started/locally/)

### 3. การเตรียมข้อมูล (Data Preparation)

**สำคัญ:** แอปพลิเคชันต้องการฐานข้อมูล ChromaDB ในโฟลเดอร์ `./chroma_db_optimized` ก่อนจึงจะทำงานได้ หากคุณยังไม่มีโฟลเดอร์นี้ หรือต้องการสร้างฐานข้อมูลใหม่ ให้ทำตามขั้นตอนนี้:

1.  ไปที่โฟลเดอร์ `notebook`
2.  ตรวจสอบว่ามีไฟล์หนังสือใน `datasets/Baronness Orczy___The Heart of a Woman.txt`
3.  เปิดและรันไฟล์ **`chuckstovec.ipynb`** ผ่าน Jupyter Notebook หรือ VS Code
    *   สคริปต์จะทำการอ่านไฟล์หนังสือ, แบ่งข้อความ (Chunking), สร้าง Embeddings, และบันทึกลงใน `./chroma_db_optimized`

### 4. การรันแอปพลิเคชัน (Running the App)

เมื่อเตรียมข้อมูลเสร็จแล้ว ให้รันคำสั่งต่อไปนี้ใน Terminal:

```bash
python -m streamlit run app.py
```

เมื่อระบบเริ่มทำงาน จะเปิดหน้าเว็บ Browser ขึ้นมาโดยอัตโนมัติที่ `http://localhost:8501`

---

## ⚙️ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

*   **⚠️ LLM โหลดไม่ได้ / หน่วยความจำไม่พอ:**
    *   แอปพลิเคชันจะยังคงทำงานได้ แต่จะแจ้งเตือนว่าทำงานในโหมด "ค้นหาอย่างเดียว" (Embedding Search) โดยไม่มีการตอบคำถามเป็นประโยคจาก AI
    *   ลองลดขนาดโมเดล หรือรันบนเครื่องที่มี VRAM สูงขึ้น

*   **❌ ไม่พบโฟลเดอร์ ChromaDB:**
    *   ตรวจสอบให้แน่ใจว่าโฟลเดอร์ `chroma_db_optimized` อยู่ในระดับเดียวกับไฟล์ `app.py`
    *   ตรวจสอบชื่อ Collection ในโค้ด `app.py` ว่าตรงกับที่สร้างใน Notebook หรือไม่ (ค่าเริ่มต้นคือ `baroness_orczy_optimized`)

*   **การล้างแคช Streamlit:**
    *   หากแก้ไขโค้ดแล้วผลลัพธ์ไม่เปลี่ยน ให้ลองล้างแคชด้วยคำสั่ง: `python -m streamlit cache clear`